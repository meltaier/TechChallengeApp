# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest


steps:
- task: AzureCLI@2
  displayName: 'Deploy'
  inputs:
    azureSubscription: 'Azuresub'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: |
      #!/bin/bash
      az login --service-principal --username 4c313cd7-b686-4c21-bae4-11c8e0dc2c6d --password Us6HTcLgEtNOCCOo8ziqY4vwB-ij57c44P --tenant 1df8fc01-cd21-47a3-9c05-066d7405ea78
      
    addSpnToEnvironment: true

    